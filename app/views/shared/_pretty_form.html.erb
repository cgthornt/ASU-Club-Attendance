<%= form_for(record, options) do |f| %>
  <% if record.errors.any? %>
  <div id="error_explanation">
    <h2>You have some errors with your submission</h2>
    <ul><% record.errors.full_messages.each do |m| %><li><%= m %></li><% end %></ul>
  </div>
<% end %>
<%= capture(f, &proc) %>
<% end %>