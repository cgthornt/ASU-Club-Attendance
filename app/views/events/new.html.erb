<%= page_title "New Event", @curr_club.name %>
<%= pretty_form_for(@event) do |f| %>
  <%= render :partial => 'form', :object => f %>
  <%= f.submit_area do %>
    <%= link_to 'Cancel', clubs_path, :class => 'btn' %>  
  <% end %>
<% end %>
